
project.name = konfirmagi

src.dir = ${basedir}/src
package.dir=${src.dir}/com/konfirmagi
lib.dir = ${basedir}/lib
doc.dir = ${basedir}/doc

build.dir = ${basedir}/build
build.classes = ${build.dir}/
build.lib = ${build.dir}/lib

test.dir = ${basedir}/tests
test.classes = ${build.classes}/tests
dist.dir = ${basedir}/dist

javacc.srcdir=${package.dir}/parser/
javacc.target=${javacc.srcdir}/parser.jj
