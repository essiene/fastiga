PinMan
    Rq => account/id/isvalid?pin=  
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}
    
    Rq => account/id/changepin?oldpin=&newpin=
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}

account#:cheque#:OK|CANCEL

PreKonfirm
    Rq => account/id/isvalid?pin=
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}

    Rq => account/id/cheque/chequenum/konfirm?status=0|1
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}

Konfirm
    CAll Variables ->
        AccountNum, ChequeNum, TransactionId

    Rq => account/id/isvalid?pin=
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}

    -Get speechfile via TransactionId

    -get confirmation action from user 0, 1, 2, 3

    Rq => account/id/cheque/chequenum/konfirm?status=0|1
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}

    /transaction/new?accountid=&chequeid&
        {
           fetch phonenumbers,
           create new transaction, [accountid, chequeid], get transaction id,
           call obelisk: transactionid, accountid, chequeid
        }

    /transaction/
    /status/
    
    #instead of doing this from the IVR, lets just call 
    #Rq => account/id/cheque/chequenum/konfirm?status=0|1|2|3&transaction_id=tid
    #ths app server handles the write to both cheque and status once here, instead of having two calls to it

    Rq => status/new?transactionid=TransactionId&status= (OK|OK_CONTACT|CANCEL|CANCEL_CONTACT|TIMEDOUT|BELOW_THRESHOLD)
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}

Record
    Rq => recorder/id/get_filename
    Re => {AppServer:Name, Version:version, Status(OK|FAIL), filename:file_name}

    Rq => recorder/id/write?from=
    Re => {AppServer:Name, Version:version, Status(OK|FAIL)}
